# 工作管理 — HrAgent

## 目标

构建多模态「AI 面试官」MVP：能**看**（摄像头画面）+ 能**听**（语音回答），主动提问、追问、评估，最终产出面试报告。

**核心原则：自动测试优先** —— 在接真实麦克风 / 摄像头之前，先用 fixture 样本数据自动跑通全流程。

**部署**：服务跑在 WSL 的 Arch Linux，音视频由 Windows 浏览器 `getUserMedia` 采集。

---

## 阶段任务清单

### 阶段 A：模块 + 单测（纯逻辑，可离线验证）✅ 已完成
- [x] A1 搭骨架：目录结构、`requirements.txt`、`config.py`、`.gitignore`、Git 仓库
- [x] A2 `app/tts.py` — edge-tts 封装（文字 → 音频字节）**已实测跑通**
- [x] A3 `app/llm.py` — 多模态 LLM 封装（文字 + 帧 → 结构化 JSON，供应商用环境变量切换）
- [x] A4 `app/stt.py` — faster-whisper 封装（音频字节 → 文字）**已实测跑通**
- [x] A5 准备 fixtures：`tests/fixtures/audio/`（3 段 edge-tts 合成语音）、`tests/fixtures/frames/`（5 张合成帧）
- [x] A6 `tests/test_units.py` — **19 个单测全绿**（不装重依赖、不联网、不需 API key）

### 阶段 B：面试引擎 + 集成测试（跑通完整面试，仍无硬件）
- [x] B0 配 `.env`（Qwen-VL-Max 密钥）→ 跑 `python -m app.check_llm` **真实验证视觉判定**
      —— 链路全通，结构性线索判对；**姿态类判断（走神）用占位图验不了，待真实照片**
- [x] B1 `data/questions.json` — 题库（开场语 + 8 道题 + 结束语）
- [x] B2 `app/interview.py` — `start` / `run_turn` / `build_report` / `render_markdown`
- [x] B3 `tests/test_pipeline.py` — 17 个集成测试（run_turn 状态流转 / 帧送达 LLM / 作弊标记 / 报告产出）
- [x] B4 `app/run_offline.py` — dry-run CLI，报告落盘 `reports/`（mock 模式已跑通）
- [x] B5 端到端 **真实** dry-run 跑通（35.6s / 3 轮），报告产出并人工检查通过

### 阶段 C：接真实硬件（最后做）
- [x] C1 `app/main.py` — FastAPI 入口 + `/api/start` `/api/turn` `/api/report` `/api/fixtures`
      **已实测跑通**：真实一轮 HTTP 请求 12.2s（STT + Qwen-VL-Max + TTS）
- [x] C2 `static/index.html` — 单页前端。**当前为模拟数据模式**：
      画面取 fixture 图片、回答取 fixture 音频，点击发送走完整 HTTP 链路
- [ ] C3 接**真实**摄像头抽帧 + MediaRecorder 录音（替换前端的模拟素材获取）
- [ ] C4 WSL 起服务，Windows 浏览器真实授权摄像头 / 麦克风验证

### 收尾
- [ ] D1 README 与实际实现对齐
- [ ] D2 全套 `pytest` 无硬件可跑（CI 友好）

---

## 里程碑

| 里程碑 | 含义 | 状态 |
|---|---|---|
| **M1** | 自动测试路径闭环（阶段 A + B 完成，`pytest` 全绿 + dry-run 出报告） | ✅ **已达成**（2026-09-18） |
| **M2** | 真实硬件跑通（阶段 C 完成，浏览器完整走一轮面试） | ⬜ 未开始 |

---

## 进度记录

> 逐条追加：日期 + 完成内容 + 备注

- **2026-09-18** — 项目立项。技术栈与分阶段方案确定；`README.md`、`工作管理.md` 创建。
- **2026-09-18** — **阶段 A 完成**。Git 仓库初始化（`main` 分支）。
  - 代码：`config.py` / `llm.py` / `stt.py` / `tts.py`；`tests/test_units.py` **19 个单测全绿**
  - 依赖装在 `.venv`（uv 创建，Python 3.13），`faster-whisper` 在 py3.13 上可用
  - fixtures：5 张合成画面帧 + 3 段 edge-tts 合成语音
  - **实测跑通**：edge-tts 合成 ✅；faster-whisper `small` 模型识别中文 ✅
  - 踩坑三条，已写进 README「环境与已知问题」：HF 需走 `hf-mirror.com` 且禁用 xet；ctranslate2 多线程崩溃 → 固定 `STT_CPU_THREADS=1`；`small` 模型有错字（架构上已容忍）
- **2026-09-18** — **LLM 选型定为 Qwen-VL-Max**（DashScope 兼容模式）。
  - 新增 `.env.example` + `config.py` 内置极简 `.env` 加载器（不加依赖）
  - 新增 `app/check_llm.py` 冒烟测试：同问题配四种画面，验证模型是否真在「看」
  - **待办**：填入密钥后跑 `python -m app.check_llm`，确认视觉判定生效
- **2026-09-18** — **B0 完成：Qwen-VL-Max 真实调用验证**。
  - 链路全通：端点、鉴权、`response_format` JSON 模式、解析兜底 ✅
  - 判对：正对镜头 ×2、画面多人（疑似作弊）、空座位（人不在）✅
  - **判不了：转头走神**。做了多轮对照实验（侧脸带鼻梁/耳朵/后脑头发、低头只见头顶、
    后脑勺无五官），模型一律判 `focused=True`。**结论：程序画的占位图验不了姿态类判断**
    —— 结构性线索（几个人/有没有人）抽象后仍保留，细粒度姿态需要真实人像先验。
  - 已在 `check_llm.py` 把该用例标为「不判定」，避免报假失败；换真实照片后改回即可
  - **意外收获**：这轮暴露了「占位图只能验链路、不能验准确度」，
    与立项时标注的限制一致，现在有了实测证据
- **2026-09-18** — **阶段 B 代码完成**（B1–B4），41 个测试全绿（19 单测 + 17 集成 + 5 新增）。
  - 引擎 `interview.py`：历史**只留文字不带图片**，否则 token 随轮数线性膨胀
  - 结论（通过/待定/不通过）**由规则算出，不交给 LLM** —— 招聘决策不该由模型拍板
  - 集成测试的桩**会校验收到的帧字节**，只有真收到「多人」图才判作弊
    → 验的是「帧确实从 `run_turn` 流到 LLM」，不是「没报错」
  - **修复：加载 Whisper 模型卡死在联网检查上。** 模型已缓存，但 huggingface_hub
    加载时仍联网查更新；国内到 huggingface.co 是**丢包挂起**而非快速失败，进程无限等待。
    表现为「跑到第一轮 STT 就不动了」。修法：先走离线模式，失败才回退联网+镜像站。
  - **教训**：此前所有 STT 测试我都顺手带了 `HF_ENDPOINT` 环境变量，所以这个坑一直没暴露 ——
    手动跑的人（不带环境变量）必踩。**测试时要按「用户会怎么跑」来跑，不能自带魔法环境**。
- **2026-09-18** — 修掉「测试偷偷走网络」：集成测试首次耗时 4.67s，查出某用例没装桩、
  真去调了 edge-tts。改成 autouse 打桩后 **0.03s**。这类问题只能靠盯耗时发现。
- **2026-09-18** — 🎉 **M1 达成：首次真实端到端跑通，产出报告**。
  命令 `python -m app.run_offline --turns 3 --no-tts`，**耗时 35.6 秒**。
  - 真实跑通：STT 转写 → Qwen-VL-Max（带图）→ 报告落盘
  - 报告结论：平均分 7.3，第 3 轮画面多人 → 走神 + 疑似作弊 → **建议人工复核**
  - **关键验证：LLM 自动纠正了 STT 错别字**。转写是「加了**索隐**之后，**想应**时间
    从2秒降到200毫秒」，LLM 追问时说的是「你提到加了**索引**就解决了问题」——
    当初「在 prompt 里声明转写可能有错别字」的设计被实测证明有效
  - **追问具备上下文连贯性**：第 2 轮问题由第 1 轮回答长出（「你提到做过性能优化…」），
    不是照题库念题
  - 报告：`reports/interview_20260918_212350.md`
- **2026-09-18** — **阶段 C 接口与前端完成（C1/C2），当前为模拟数据模式**。
  - `app/main.py`：`/api/start`、`/api/turn`、`/api/report`、`/api/fixtures`、`/api/status`
  - `static/index.html`：单页前端，无框架。画面取 fixture 图、回答取 fixture 音频，
    **点击发送走完整 HTTP 链路**（FormData + Blob，与真实 MediaRecorder 产出一致）
  - **实测**：真实一轮 HTTP 请求 **12.2s**，作弊判定正确触发
  - **抓到并修掉一个真 bug**：`/api/turn` 写成 `async def` 时，
    `tts.synthesize` 内部的 `asyncio.run()` 会抛
    `cannot be called from a running event loop`，整轮 502。
    改用**同步 def**（FastAPI 丢线程池，那里没有运行中的循环），
    并用 `audio.file.read()` 同步读替代 `await audio.read()`
  - **测试盲区教训**：原来的接口测试把 `run_turn` 整个打桩了，
    于是**端点自身的同步/异步性质从没被覆盖**，bug 溜过去了。
    补了 `real_engine_client` 夹具 —— 只打桩最底层叶子依赖，
    让 `tts.synthesize` 真的执行 `asyncio.run()`。已反向验证该测试能抓住此 bug
  - 测试数：56 个（19 单测 + 17 集成 + 15 接口 + 5 新增）
- **2026-09-18** — **代码审查，修掉 2 个严重 bug + 7 个前端问题**。（测试 56 → 78）
  - **严重①：`bool("false")` 是 True。** 模型偶尔把布尔写成字符串，
    于是「没有作弊」被读成「**作弊**」—— 招聘场景里最不能出错的方向。
    同理会误判 `focused`（走神）和 `should_end`（提前结束面试）。
    修法：新增 `_as_bool()` 识别 true/false/是/否/0/1 等写法，
    **认不出来时走安全默认**（不指控作弊、不判走神、不提前结束）
  - **严重②：模型把 `attention` 返回成字符串时 `_normalize` 直接崩。**
    `obj.get("attention") or {}` 对非空字符串不生效 → `.get` 抛 AttributeError → 整轮 502。
    这违背了 `parse_reply`「绝不抛异常」的对外承诺。
    修法：`_as_dict_or_text()`，字符串当作说明文字保留而不是丢掉
  - 顺带修：分数 `7.0` / `"7.5"` / `"8分"` 原本一律变 None，现在能正确解析
  - **前端 7 处**（由独立 agent 审出，其中 3 处我原本也怀疑过）：
    ① `finished` 后的按钮禁用被 `finally` 覆盖 → 结束后还能继续提交
    ② 开考瞬间「查看报告」就启用 → 0 轮时点击必报 409
    ③ 「开始面试」一生只能点一次 → 结束后无法开第二场
    ④ 12 秒请求期间下拉框未锁 → 屏幕显示的帧与提交的帧可能不一致（看着像作弊检测失效）
    ⑤ 错误提示的红色样式被 `finally` 抹掉，三处 catch 全中招
    ⑥ 取素材的 fetch 没查 `res.ok` → 404 的 JSON 被当成音频发给后端
    ⑦ `playB64` 播不出来时彻底静默 + objectURL 泄漏 + Audio 对象可能被 GC
  - **后端补守卫**：`/api/turn` 增加「已结束」检查 —— 前端禁用按钮不够，
    直接调 API 同样得挡住，否则会在已结束的面试上继续记分
  - **架构性修复**：前端状态从散落的裸变量改成单一 `state` 对象 +
    `refreshButtons()` 统一派生按钮可用性，根除「一处禁用、另一处又启用」的互相覆盖
  - 验证方式：JS 过 `node --check`；新增回归测试并在真实服务上确认守卫返回 409
- **2026-09-18** — **后端独立审查，修掉 2 高 5 中 4 低**。（测试 78 → 98）
  - **高①：状态先推进、TTS 后执行 → 失败后同一轮被记两次且问答错配。**
    `run_turn` 原本先写 `records`/`history` 再调 TTS。TTS 失败（限流/断网，国内常见）
    → 接口 502 但这一轮已入库；用户重试 → 变成两轮，且报告里那轮的「问题」
    **从未被念出来过**，候选人答的是上一题，分数/走神/作弊统计全部错位。
    修法：把 TTS 挪到提交状态**之前**，失败时 state 保持原样。
    `start()` 同样处理（否则开场合成失败会让 state 停在半路）
  - **高②：`load_questions()` 不校验顶层类型 → 题库写成数组就 500。**
    `json.loads('["a","b"]')` 是合法的，光 catch 异常挡不住，随后 `bank.get()` 抛
    AttributeError，与 docstring「不让面试开不了场」的承诺相反。
  - **中③：`/api/start` 无 try/except** → TTS 失败返回裸 500 + 非 JSON，
    前端只显示 "500 Internal Server Error"，完全看不出是 TTS 的问题
  - **中④：`closing` 无兜底** → 缺该键时结束语是空串，候选人在**静音**中结束面试
  - **中⑤：`check_llm.py` 零用例也报 ✅ 并 exit 0。** —— 我自己的验证工具
    在**零证据**下给出通过结论。典型触发：按它的建议换成真实照片但文件名对不上，
    5 个用例全被跳过仍报成功。修法：`rows` 为空时返回 2 并列出期望文件名
  - **中⑥：TTS 无超时且在持锁期间调用** → edge-tts 连上后不回数据时永久挂起，
    锁永不释放，`/api/status`、`/api/start`、`/api/report` 全部永久无响应且无报错。
    修法：加 `TTS_TIMEOUT`（默认 30s）
  - **中⑦：分数不设上下限** → 换模型/端点后对方若按百分制给分（85），
    平均分变 85，结论直接失真成「建议通过」。修法：夹在 0-10
  - **低⑧：`_FALSE_WORDS` 含空串** → `focused=""` 被判成走神，
    正好违反 `_as_bool` 自己声明的「不轻易给候选人扣帽子」
  - **低⑨：非结构化回复被 TTS 原文念出。** 模型被 max_tokens 截断时返回半截
    ```json {...}，这段乱码会被**念给候选人听**并写进报告。改为安全追问，
    原文存 `_raw`；同时把 `_parsed` 透出到 API，前端显示「本轮模型输出异常」
  - **低⑩：502 回传异常原文** → 上游报错体可能带请求头、内网地址。改为只回类型，
    完整信息写服务端日志
  - **低⑪：`--no-tts` 仍在合成开场白** → 离线时照样在第一步中断。改为真正替换掉合成
- **2026-09-19** — **美化前端 + 精简报告**。（测试 98 → 104）
  - **报告精简**：`render_markdown` 逐轮从「问题全文+回答全文+点评+画面+帧数」
    压成**一行**（轮次 · 得分 · 一句点评 · 异常标记）。实测 850+ 字符 → **204 字符**。
    顶部摘要去掉「生成时间」「问答轮数」两行噪音
  - **`build_report` 新增 `verdict` 枚举**（pass/hold/fail/review/unknown），
    供前端给结论横幅着色 —— 避免前端拿中文串做关键词匹配
  - **前端整体视觉升级**：顶栏加 logo 与渐变、聊天改**左右气泡**（面试官左/候选人右，
    带入场动画）、按钮与下拉的 hover/focus/disabled 质感、状态栏与空状态微调。
    全部沿用原 CSS 变量体系，只加样式不动 JS 结构
  - **报告区重做**：不再 dump 黑底 markdown，改成结构化渲染 ——
    大号平均分（按分档着色）+ 结论横幅 + 三张统计卡 + 逐轮一行列表
    （得分 chip 按 8/6 分档着色、一句点评省略号截断、走神/作弊 tag）
  - **分档阈值前后端对齐**：前端 `scoreClass` 与后端 `_recommend` 都用 8/6，
    否则会出现「报告说通过、每轮却标红」的自相矛盾
  - `.md` 字段仍由接口返回（`scripts/curl_demo.sh` 与离线 `run_offline.py` 仍用它），
    只是前端不再展示
  - 新增测试：逐轮一行里**不含**问题/回答原文（锁住「简短」这个要求）、`verdict` 枚举
- **2026-09-22** — **接入真实摄像头/麦克风 + 长按发送 + 预设答案降级**（C3 完成）。（测试 104 → 109）
  - **题库加 `answer` 字段**：q1–q5 参考答案 + 新增 q9「为什么选软件测试」（岗位动机）
  - **降级逻辑**（`interview.run_turn`）：STT 转出空串时，按轮次取
    `questions[N-1].answer` 顶上，`TurnResult`/`TurnRecord` 加 `fallback` 标记。
    **关键判断**：「长按太短」和「麦克风没收到声音」的结果都是转出空串，物理上等价，
    所以两种降级统一在**后端**处理，前端不用分别判断
  - **降级标记全链路透出**：API 加 `fallback` 字段 → 前端气泡显示
    「⚠️ 本轮没录到有效语音，已使用题库预设答案」→ 报告的逐轮行标「预设答案」。
    必须说清楚，否则会被当成候选人的真实回答
  - **前端真实采集**：`getUserMedia` 取摄像头+麦克风 → `<video>` 显示（镜像）+
    `MediaRecorder` 录音；抽帧用 `canvas.drawImage(video)` → JPEG，**与真实摄像头
    取帧同一套代码**，将来换 MP4 或其他源不用改
  - **长按交互**：`pointerdown` 开始录音+抽帧，`pointerup`（挂 document，移出按钮也能收到）
    停止并发送；按住时按钮变红呼吸。`< 150ms` 视为误触不发送，避免白吃一轮
  - **真实/模拟开关**：顶栏分段控件，真实模式默认。两条路径的 Blob 来源不同，
    但共用 `sendTurn()` 组装/发送/渲染
  - **实测**：空音频 → `fallback=True` 且用上 q1 预设答案；真实音频 → `fallback=False`；
    报告逐轮标出降级轮次
  - **未验证**：真实摄像头/麦克风需要浏览器授权，**本机无浏览器，这部分只能靠用户实测**
- **2026-09-22** — **修复 STT 幻觉 bug（用户实测发现）**。（测试 109 → 118）
  - **症状**：真实麦克风回答被识别成 `请用简体中文转写。`（重复出现），偶尔 `OK 放下`
  - **根因**：`请用简体中文转写。` 正是 `STT_INITIAL_PROMPT` 的原文。
    **Whisper 在无效/极短音频上不会返回空，而是幻觉出固定话术**，
    最常见的就是把 initial_prompt 原样吐回来。实测复现：
    **0.3 秒音频 → `请用简体中文转写。`**
  - **为什么降级没兜住**：降级只在「STT 返回空串」时触发，
    而幻觉出来的是**非空垃圾**，所以判断不成立 —— 它会冒充成正常回答进报告，
    比空串更危险
  - **修法（两层）**：
    ① `STT_MIN_SECONDS`（默认 0.8 秒）—— 音频太短直接不采信
    ② `_looks_like_hallucination()` —— 命中提示词回吐或已知幻听话术则清成空串
    两者都返回空串，于是**现有的降级逻辑自然生效，`run_turn` 不用改**
  - **误杀防护**：初版把「订阅/转发/打赏」等**单词**也当幻觉，
    测试立刻抓到 `我今天订阅了一个技术专栏` 被误判。已收紧为
    「组合起来才成立」的完整话术 —— **误杀真回答比漏判更糟**
  - **前端加诊断**：降级时显示「录音 X 秒 / Y KB，抽到 N 帧」，
    一眼看出是「按太短」还是别的原因
  - 实测：0.3 秒音频 → 降级；1.5 秒 → 正常识别不误伤
- **2026-09-22** — **修复：切到真实模式后模拟图仍在显示**。
  - **根因**：CSS 覆盖。`.camera img { display: block }` 的优先级**高于**
    `hidden` 属性依赖的 UA 样式 `display: none`，所以 `cam.hidden = true` 形同虚设
  - 这个 bug 一直潜伏：以前 `showFrame()` 只把 `hidden` 设为 `false`，从不设 `true`，
    所以从没暴露；这次模式切换第一次真正用到了 `hidden = true`
  - **修法**：加全局兜底 `[hidden] { display: none !important; }`
    （一次覆盖全部 6 个动态隐藏元素：cam / camVideo / camPlaceholder /
    realControls / simControls / sendBtn）
  - **教训**：用 `hidden` 属性控制显隐时，只要同元素有作者样式的 `display` 声明就会失效
- **2026-09-22** — **在 Windows 本机（无 WSL）跑通全流程 + 修掉一个「新机器装不上」的隐蔽 bug**。
  - **环境差异**：这台机器是 Windows 11 + Python 3.13.2，**没装 WSL**，与原来的
    WSL/Arch 开发环境不同。实测三处都能扛住：`ctranslate2` 4.8.2 / `faster-whisper` 1.2.1
    在 py3.13 有现成轮子；`STT_CPU_THREADS=1` 的规避照样有效；edge-tts 与 DashScope 都通
  - **验证结果**：118 个测试全绿（0.6s，**未装** faster-whisper/edge-tts/openai 也能跑，
    印证了「重依赖懒加载」的设计）；`/api/start` 真实合成 66KB MP3（1.4s）；
    STT 真实转写 3 段样本；**真实端到端 3 轮出报告**（平均 7.0，第 3 轮多人画面 →
    走神 + 疑似作弊 → 建议复核）
  - README 记录的 STT 错字**全部原样复现**：`Python→Pathong`、`日志→日制`、`索引→索隐`、
    `源码→原码`；并且 LLM **又一次自动纠正了错字**（转写「索隐」→ 追问说「你提到加了索引」），
    当初那条 prompt 设计在本机再次得到验证
  - **修掉一个真 bug：「先离线、失败再联网」的兜底是死代码，新机器永远下不了模型。**
    根因：`huggingface_hub` 在**首次 import 时**就把 `HF_HUB_OFFLINE` / `HF_ENDPOINT` /
    `HF_HUB_DISABLE_XET` 读成模块常量（`constants.py`），之后再改 `os.environ` 一律无效。
    而 `stt.py` 恰好在 `from faster_whisper import ...` **之前**把 `HF_HUB_OFFLINE=1`
    写进环境变量 → 常量被锁死成 True → 回退分支里 `os.environ.pop()` 毫无作用，
    联网那步照样被 `OfflineModeIsEnabled` 挡住。**「先离线」实际变成了「永远离线」。**
    回退的另一半同样失效：镜像地址是 import 之后才 `setdefault` 的，压根没赶上
  - **为什么一直没暴露**：模型早就缓存好了，第一次「离线加载」直接成功，走不到回退分支。
    只有**新机器 / 清了缓存**才会踩到 —— 实测把 `HF_HOME` 指向空目录，立刻
    `LocalEntryNotFoundError: outgoing traffic has been disabled`
  - **修法**：放弃环境变量，改用 faster-whisper 原生的 `local_files_only` 参数
    （第一次 True 纯本地，失败再 False 联网）；`HF_ENDPOINT` / `HF_HUB_DISABLE_XET`
    改成在任何 import **之前**用 `setdefault` 设好。实测空缓存 + `tiny` 模型能真正走镜像下载
  - **测试盲区（本轮的教训）**：原有 5 个测试专门锁定旧实现，断言 `HF_HUB_OFFLINE` 的
    设置与还原 —— 但它们**打桩的正是 `_build_model`，也就是含 import 的那一层**，
    于是「import 时机锁死常量」这件事从未被覆盖。**测试全绿，行为却是坏的。**
    与之前「打桩 `run_turn` 导致端点同步/异步性质没被覆盖」是同一类错误：
    **打桩位置太靠上，把要验的东西一起打掉了**。已把这 5 个测试改写成断言
    `local_files_only` 的取值与镜像地址的设置时机
- **2026-09-22** — **前端暗色主题改造 + 交互增强**（`static/index.html`，测试仍 118 个）
  - **起因**：界面原本是**浅色**（`--bg:#f4f6fa` + `--panel:#ffffff`），只有摄像头框是深色块。
    目标是把整个界面做成真正的暗色主题
  - **暗色的层次不靠投影，靠表面阶梯**：浅色里 `0 4px 14px rgba(16,24,40,.05)` 能让面板浮起来；
    暗色里背景亮度本就低，再叠 4% 黑，L\* 只动 1–2，肉眼无感。改成三层配合 ——
    **表面阶梯**（page/sunken/panel/raised/raised-2 管「面」）+ **发丝描边**（管「边」）+
    **顶边 1px 内高光**（管「受光」）。投影只留给吸顶栏、分段控件、logo 三处真漂浮的元素
  - **绝不用纯黑**：① 纯黑上放浅字会 halation（发糊）；② 没有比黑更黑的层 → 做不出「下沉层」，
    而暗色 UI 最需要下沉层；③ 会和摄像头井打架。基色取 `#0b0f16`，色相 220 与
    `.camera` 原渐变同族
  - **主次关系要重排，不是照搬**：浅色里「更深 = 更重要」，暗色里翻成「更亮 = 更重要」。
    具体重映射了 `.round .cmt`（原 `#3f4859` 比 `--muted` 还深 → 主文本 → 改 `--text`）
  - **对比度全部实算过**，两个结论值得记：
    ① **主按钮填充 `#3b6cf6` 一个字都不能改亮** —— 白字在其上正好 4.50:1 压线过 AA，
       更亮的候选 4.23 / 3.90 / 3.39 全不达标。所以 hover 用**发光**而不是「变亮」
       （实际改成了变暗 `--accent-active`，对比度只会更高）
    ② **深底上的 accent 文字必须换 `--accent-ink`** —— `.modeswitch button.on` 原本用
       `var(--accent)`，在选中滑块底色上只有 **3.37:1**，远不够 12.5px 正文要求的 4.5
  - **顺手修掉一个既存的 AA 违规**：录音按钮 `#e05252` 上的白字只有 **3.82:1**
    —— 这是**浅色主题里就存在的**问题，改暗色时一并压到 `#c9403f`（4.90:1）
  - **⚠️ 最高危的一处：媒体查询被后面的新规则悄悄吃掉。**
    `@media (max-width:880px)` 原本在样式表**中间**（第 128 行），而媒体查询**不加优先级** ——
    同优先级下谁在文档里靠后谁赢。所有新增 CSS 都会追加到它后面，于是任何一条
    `#log { flex: ... }` / `.panel-chat { overflow: ... }` 都会盖掉里面的单栏特判，
    退回「聊天区被压成 0 高」的老 bug。**已把整块移到样式表末尾（614 行中的第 593 行）**，
    并写了注释说明为什么必须留在最后
  - **交互增强**：`color-scheme: dark`（修掉 `<select>` 白弹层 —— 那是系统绘制的，纯 CSS 修不了）、
    「面试官正在思考」占位气泡（一轮真实调用要等 10-20 秒，此前聊天区毫无反馈）、
    hover 发光 + 抬升、按压 `scale(.99)`、禁用态改明确配色（原来 `opacity:.42` 在暗底糊成泥）、
    报告逐轮错开入场、摄像头 live 状态环、`prefers-reduced-motion` 降级
    （**转圈必须保留** —— 它承载「正在处理」这个信息，停掉等于丢信息）
  - **发现并修掉一个真 bug：真实模式下摄像头一失败，画面区就是全黑的空框。**
    `showFrame()` 藏掉占位、`setMode("real")` 藏掉模拟图、视频又没出来 —— 三个都藏了。
    用户只能看到「摄像头是黑的」，完全无从判断原因（**用户实测反馈的就是这个现象**）。
    修法：加 `showCameraNotice()`，失败/不支持时给出具体提示。
    另外补了 **`getUserMedia` 在权限弹窗等待期间是「挂住」的**（既不成功也不失败），
    这段窗口期画面区同样空着 —— 所以进真实模式时先摆一句「正在请求摄像头/麦克风权限…」。
    顺带：`srcObject` 是在元素 `display:none` 时赋值的，单靠 `autoplay` 属性有时不起播，
    补了显式 `camVideo.play()`
  - **验证方式（本轮最大的方法收获）**：本机此前被认为「无浏览器只能靠肉眼」，
    实际 **Chrome 就在机器上**，用 `--headless=new --screenshot` 可以真的截图看。
    更关键的是做了个**样式探针页**：把同一个 `<style>` 内联进一个临时 HTML，
    把所有组件状态（三种按钮态、四种 tag、四种分数着色、四种 verdict 横幅、
    正常/flagged 明细行、thinking 气泡、错误态状态栏……）一次性渲染出来截一张图 ——
    **一个 CSS 改动能覆盖到的状态，比手工点击页面试十遍还全**。
    探针页当场就暴露了上面那个「摄像头空黑框」bug（真实模式的框是全空的，
    模拟模式的框有「尚未开始」提示，两张一对比问题直接现形）
  - **仍需人眼在真浏览器确认的**：真实摄像头/麦克风授权（C4）、
    `<select>` 弹层是否转深色、Tab 键焦点环、800px 单栏、系统「减少动态效果」
  - **按钮细化**：「开始面试」「发送回答」「按住说话」三个主操作按钮统一成蓝色渐变
    （共用 `button.primary` / `button.hold` 那套实心渐变外观）；「按住说话」文字前加麦克风图标，
    「查看报告」文字后加文档图标（内联 SVG + `currentColor`，跟随文字色与字号缩放，不引图标库）
  - **⚠️ 渐变与禁用态的耦合靠源码顺序，不是优先级**：`button:disabled` 与 `button.primary`
    优先级完全相同（都是 0,1,1），所以「禁用时显示扁平灰」全靠 `button:disabled` 写在更后面。
    规则一旦挪到最后，禁用按钮就会亮着渐变（且不报错）。已在 `button.primary` 处加注释锁住
- **2026-09-22** — **「按住说话」改成「点击开始 / 再点结束」**。
  - **为什么改**：回答一段话要十几到几十秒，全程按住鼠标既别扭、又容易中途不小心松开
  - **实现**：`pointerdown` + document 上的 `pointerup`/`pointercancel` 三条监听，
    换成 `holdBtn` 上的一条 `click` 切换（`startRecording` / `stopRecording`）。
    `MIN_HOLD_MS` 改名 `MIN_RECORD_MS`（150ms → 300ms），语义从「按得太短」变成「连点两下的误触」
  - **必须一并处理的边界情况（改点击模式才出现的）**：原先手指按着不可能去点模式切换，
    改成点击后「点了开始 → 切到模拟数据」变得很容易发生。而 `closeCamera()` 会把音轨停掉，
    若还走 `stopRecording()` 就会**拿着一个已死的流去发送**。
    新增 `cancelCapture()`（丢弃采集、不发送），在 `setMode` 切走真实模式时调用
  - **CSS 顺带**：`.hold` 的 `touch-action: none` 是为长按准备的，改点击后会挡住触摸滚动，
    换成 `manipulation`（顺带去掉移动端点击延迟）
  - **验证方式**：这次除了静态检查，还做了**运行时验证** —— 复制一份页面到 `static/`，
    打桩底层采集与发送，用 JS 程序化点击并 dump 状态机（临时文件用完即删，不进仓库）。
    三条路径实测：快速连点(80ms) → 不发、状态栏提示「录音太短」；
    正常间隔(600ms) → 正常发送；录音途中调 `cancelCapture()` → 状态复位且不发送。
    **注意一个测试陷阱**：发送后 `refreshButtons()` 会把按钮禁用（无摄像头时 `cameraReady=false`），
    于是「快速连点」根本没触发 —— 测出来的是「禁用按钮不响应」，不是「误触保护」。
    必须在每次点击前重新放开 `disabled`，才测到真正想测的东西
- **2026-09-22** — **修掉自己上一轮埋的坑：暗色表面阶梯步长太小，气泡看不见**（用户实测反馈）。
  - **症状（用户原话）**：「全部文字堆在框内，分不清谁说话」
  - **排查过程**：先用真实渲染路径（拦截 `api()` 喂真数据）截图 → 看着像裸文本。
    但 `getComputedStyle` 量出来**一切正常**：`padding:10px 14px`、`background:rgb(26,32,41)`、
    `border:1px solid`、`border-radius:4px`，规则也都在（111 条）。
    → 于是改成**量截图像素**，真相才出来
  - **根因**：气泡**渲染是对的，只是对比度低到看不见** ——
    面试官气泡 vs 面板底色 **1.063**、候选人气泡 **1.099**、两者彼此 **1.034**。
    我上一轮定表面阶梯时写的理由「亮度接近 0 时人眼更敏感，1.10 已清晰可辨」**只在发丝线上成立**：
    **小面积（1px 描边）1.06 能看见，大面积填充（气泡）完全不行。**
    同一个错误还波及 `--raised`（明细行、幽灵按钮，1.06）
  - **修法（全部用 WCAG 公式实算后取值）**：
    - 新增气泡专用令牌：`--bubble-q: #262f3d`（灰，1.33）、
      `--bubble-a: #2a52ad`（蓝，2.49）、`--bubble-a-ink: #eef2ff`（5.99，过 AA）
    - `--raised` 1.06 → **1.26**，`--raised-2` → **1.41**（明细行、幽灵按钮一并受益）
    - 实测复核：灰气泡 vs 蓝气泡 **1.03 → 1.87**，两个方向一眼分得开
  - **教训（比 bug 本身更重要）**：**验证 CSS 不能只看「样式有没有生效」，必须量「生不生效得出来」。**
    `getComputedStyle` 只能证明规则被应用，证明不了人眼看得见 ——
    我上一轮的样式探针就是这么漏掉的（探针里组件挨得近，注意力全在「渲染没渲染」上）。
    现在有了两条可复用的检查手段：**量计算样式**（判断规则是否生效）
    + **量截图像素对比度**（判断是否可见），两者要一起用
  - **交互结构改动**：
    - **AI 点评独立成卡**：原先「点评」和「画面」是挂在候选人气泡里的两行 `.note` 小字，
      既挤在气泡内、又因为右对齐被读成「候选人自己说的」。改成横跨整行的琥珀色卡片
      （`--note-bg/-line/-ink`），挂在 `#log` 上、放在每组问答下方。
      灯泡图标沿用内联 SVG 约定（没用 emoji，与已有的麦克风/文档图标保持一致）
    - **降级/异常提示留在气泡里**：这两条讲的是「这轮是怎么来的」，属机制说明，和回答是一体的
    - **顶栏切换器改成小标签**：去掉容器底色与外框，两个选项各自成小胶囊（`border-radius:full`），
      选中态 = `--accent-line` 底 + `--accent-ink` 字（1.69）。
      这里**特意没用 `--accent-soft`** —— 它在顶栏上只有 1.12，正是刚踩过的同一个坑
    - **说话人标签加图标**：面试官 = 机器人头、候选人 = 人形，候选人标签同时改用 `--accent-ink`
      偏蓝，进一步强化左右区分。三个图标（含点评的灯泡）都是内联 SVG 而非 emoji ——
      emoji 在各平台字体不同、大小与基线压不准，混在文字标签里会很跳。
      `who` 的文字走 `createTextNode` 而非拼进 `innerHTML`，避免将来引入注入面
  - **一个截图陷阱（再次印证「工具的结果也要怀疑」）**：中途有一张截图整体发虚，
    看着像全局透明度出了问题。实际是**抓在了 `pop` 入场动画的中途** ——
    `--virtual-time-budget` 能推进定时器与网络，但**推不动合成器的 CSS 动画时钟**。
    加 `--force-prefers-reduced-motion`（动画被压到 0.01ms）重截即清晰。
    这条顺带把「减少动态效果」的降级路径也验了
- **2026-09-22** — **报告区改为「综合得分卡片」**（用户要求：大数字平铺 → 圆环 + 标签 + 三块信息）
  - **结构**：`综合得分` 标题 → 圆形进度环（浅灰底环 + 蓝色已得分弧，环心是分数 + `/ 10`）
    + 橙色状态标签 → 三块小标签并排（答题轮数 / 走神轮次 / 疑似作弊）
  - **圆环实现**：SVG `<circle>` + `stroke-dasharray`＝整圈周长、`stroke-dashoffset` 决定露出多少。
    半径 52 + 描边 10 → 外径 57，正好落在 120 的 viewBox 内。
    用 dashoffset 而不是改 dasharray，是为了让弧两端始终是圆头（`stroke-linecap: round`）
  - **⚠️ 顺手修掉一个真 bug：环的读数曾经可能显示成 0。**
    初版写法是「先把 offset 设成空环，再 `requestAnimationFrame` 改回真实值」来做扫入动画。
    但 **rAF 在标签页切到后台时不触发** —— 那样环会永远停在空值上，
    **一个 6 分的面试会显示成 0 分**。修法：最终值写进 `stroke-dashoffset` **属性**兜底，
    扫入动画改用 Web Animations（`element.animate`）。动画失效最多没有扫入效果，读数不会错
  - **两个标签名没照抄用户原话，理由**：用户写的是「出错轮次 / 模拟作弊」，
    但后端字段是 `distracted_turns`（走神）与 `cheating_turns`（**疑似**作弊）。
    「模拟作弊」会读成「模拟出来的作弊」，与数据语义相反，故保留「疑似作弊」。
    「答题轮数」照用户的改了
  - **量圆环时踩的坑（方法论）**：先用「蓝色像素的角度跨度」量覆盖率，测出 88%，
    与期望 60% 差很远，一度以为几何算错了。实际是**稀疏的抗锯齿像素把角度跨度撑大了** ——
    换成**按角度分箱统计密度**再取主弧，得到 204°(56.7%)，与 60% 吻合（差额是圆头线帽被阈值裁掉）。
    教训：**用像素量几何时，不要用「最小/最大」这种被离群点主导的统计量**，
    要用对离群点稳健的统计量（分箱密度、中位数、拟合）
- **2026-09-22** — **逐轮明细改为卡片式**（用户要求：纯文本列表 → 每轮一张独立卡片）
  - **结构**：卡片纵向两行 ——
    头部（`第 N 轮` + **圆形分数徽章** + 右侧状态标签）／正文（评价文字，整段换行）
  - **圆形徽章四档配色**：高(≥8)=绿、中(≥6)=橙、低(<6)=灰、无分数=虚线空心。
    **低分走中性灰是用户明确要求的**（原先是红色 `--danger`）。
    这里有个坑：如果低分和无分数都用灰，两者会**长得一模一样** ——
    所以无分数那档改用虚线空环，和「实心灰 + 数字」区分开
  - **顺带清理**：`.chip` 系列（4 档）连同 `.round .idx` 的固定宽度一起删掉，
    换成 `.badge-score`；`.round .cmt` 去掉 `nowrap + ellipsis`（原来单行截断），
    改成整段换行 —— 卡片式布局里正文本来就该展开
  - 沿用已有的 `scoreClass()` 分档（阈值 8/6，必须与后端 `interview._recommend` 一致），
    **没有改阈值逻辑**，只改了这四个档位映射到的颜色
  - 实测截图覆盖四档：9(绿) / 7(橙) / 4(灰) / —(虚线)，外加一张 `flagged` 红底卡
  - **踩到一个坑**：`onHoldStart`/`onHoldEnd` 原本用 `holdBtn.textContent = "..."` 切换录音文案，
    而 `textContent` 会**把按钮里的图标一起抹掉**。修法是把文案单独包一层
    `<span id="holdLabel">`，JS 只改这个 span —— **凡是按钮里要放图标的，
    文案都必须能独立定位**，不能靠 textContent 整体替换
  - **渐变按钮的 hover 必须单独写**：基类 `button:hover:not(:disabled)` 的优先级更高，
    会把 `background` 压成纯色，渐变就没了。所以补了 `button.primary:hover` /
    `button.hold:hover` 两条（往更暗方向走，对比度只增不减，不会跌破 AA）

---

## 关键决策记录

| 日期 | 决策 | 理由 |
|---|---|---|
| 2026-09-18 | 音频用「点击录制 → 点击发送」，不做 VAD 静音切割 | 保持最简单，避免引入流式音频复杂度 |
| 2026-09-18 | 引擎设计成纯函数 `run_turn(audio_bytes, frames)` | 自动测试与线上共用同一入口，接硬件零改动 |
| 2026-09-18 | 音频先转文字再进 LLM，不直接喂音频 | 「听」「看」两条链路解耦，最稳 |
| 2026-09-18 | 不引入 LangChain，用纯 Python 状态机 | 减少依赖，逻辑透明 |
| 2026-09-18 | STT 固定 `cpu_threads=1` | ctranslate2 多线程在本机崩溃，稳定优先于速度 |
| 2026-09-18 | STT 加 `initial_prompt` 简体引导词 + 标点归一化 | Whisper 会输出繁体、半角标点，下游看着别扭 |
| 2026-09-18 | 重依赖（whisper/openai/edge-tts）全部在函数内懒加载 | 单测不装重依赖、不联网也能全绿 |
| 2026-09-18 | **LLM 选型定为 Qwen-VL-Max**（DashScope 兼容模式） | 国内直连，中文强；OpenAI 保留为可切换项 |
| 2026-09-18 | 配置支持根目录 `.env`（手写十几行加载器） | 免去每次 export，又不值得为此加 python-dotenv 依赖 |
| 2026-09-18 | STT 加载模型**先走离线**，失败才联网 | 已缓存还联网检查 → 国内丢包挂起，进程假死 |
| 2026-09-18 | 面试结论由**规则**算，不用 LLM | 招聘决策不该交给模型，且便于测试 |
| 2026-09-18 | 对话历史只留文字，图片不进历史 | 否则 token 随轮数线性膨胀 |

---

## 风险 / 待定

- **走神检测尚未验证**：占位图验不了姿态判断（见上）。**需要真实照片**才能确认
  Qwen-VL-Max 能否识别「转头/低头不看屏幕」。若换真实照片后仍判不出，才需要考虑调 prompt
  或换模型 —— 目前没有证据支持任一结论
- **STT 中文精度**：`small` 模型实测有明显错字（Python→Pathong、索引→索隐）。已用「LLM 容忍错别字」兜住，若要根治需换 SenseVoice / FunASR
- **STT 速度**：受 `STT_CPU_THREADS=1` 限制，本机推理偏慢（实测单段约 6s 含模型加载）。
  **未测过 `cpu_threads=2/4`** 是否稳定 —— 若能稳定可望提速数倍，值得一试
- fixtures 画面帧是**程序画的占位图**，真正验证视觉判定需要用真实照片替换
- **端到端已跑通，但仍未用真实照片/真实麦克风验证过**。当前报告里的
  「走神/作弊」判定用的是占位图，只能证明链路通，不能证明判定准
- 单轮成本：3 轮约 35s（含 STT 约 18s + LLM 约 15s）。真实面试 5-10 轮会更久，
  且每轮带图调用 LLM，**注意 DashScope 用量**
- **平台**：立项时定的是「服务跑 WSL/Arch」，但 2026-09-22 在 **Windows 11 原生
  python 3.13.2** 上完整跑通了一遍（无 WSL）。`requirements.txt` 里的依赖在 Windows
  都有轮子，`run_offline` / FastAPI / 前端都能跑。**注意 `STT_CPU_THREADS=1` 那条
  规避是在 WSL 上得出的结论，Windows 上是否同样需要、能否调大提速，尚未实测**
- **网络（本机实测）**：hf-mirror.com 通，但**大文件会中途挂起**（实测 `model.bin`
  下到 10 MiB 后停住十几分钟，进程不退出也不报错）。pip 装包时也遇到过 SSL EOF。
  **下载模型失败时先怀疑网络，别怀疑代码** —— 重跑一次通常能续上
